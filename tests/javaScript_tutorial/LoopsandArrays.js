// array operations: push, pop, shift, unshift, splice, slice, forEach, map, filter
const fruits = ["apple", "banana", "cherry", "date"];
fruits.push("guava"); // adds "guava" to the end
fruits.map(fruit => console.log(fruit.toUpperCase())); // converts all fruits to uppercase
// map deiniition: The `map` method creates a new array populated with the results of calling a provided function on every element in the calling array.
